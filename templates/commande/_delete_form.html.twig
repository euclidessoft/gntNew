<form method="post" action="{{ path('commande_deletein', {'id': produit.produit.id}) }}" onsubmit="return confirm('Etes-vous sur?'); "style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.produit.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i></button>
</form>
